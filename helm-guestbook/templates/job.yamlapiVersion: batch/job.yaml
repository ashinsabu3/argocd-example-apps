apiVersion: batch/v1
kind: Job
metadata:
  name: immutable-job-test
  labels:
    app: immutable-job
spec:
  template:
    metadata:
      labels:
        app: immutable-job
    spec:
      restartPolicy: Never
      containers:
        - name: echo
          image: busybox
          command: ["sh", "-c", "echo 'Hello from first job!' && sleep 30"]
