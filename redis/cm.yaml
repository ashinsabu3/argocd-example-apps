apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-db-secret
data:
  username: <+secrets.getValue("account.secretfromvault")>-<+secrets.getValue("account.secretfromvault")>-<+secrets.getValue("account.vaultsecret2")>
  username2: <+secrets.getValue("account.testAshin")> #<+secrets.getValue("db-username-2")>
